import { API_URL, SECOND_BACKEND_URL } from '@env';
export const base_url = API_URL || 'http://localhost:3001';
export const blog_url = SECOND_BACKEND_URL || 'http://localhost:3000';
export const seconde_url = SECOND_BACKEND_URL || 'http://localhost:3000';